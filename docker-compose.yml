services:
  web:
    build: .
    ports:
      - "5000:5000"
    environment:
      MINDWATCH_SECRET_KEY: change-me-in-production
      MINDWATCH_DB_FILE: /app/data/mindwatch.db
    volumes:
      - mindwatch_data:/app/data
    command: gunicorn -b 0.0.0.0:5000 app:app

volumes:
  mindwatch_data: